# Copy to .env.local and fill in values

# EventPark API (from Railway after deploy)
VITE_API_URL=https://your-api.railway.app

# Paystack public key (for client-side inline checkout)
VITE_PAYSTACK_PUBLIC_KEY=pk_live_...
